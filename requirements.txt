# The exact package versions this milestone was tested against.
#
# Generated from the working virtualenv with `pip freeze`, so a build from
# this file installs what the 1,653-test suite and the evaluation suite
# actually ran on — not whatever happens to be newest on the day. The
# Dockerfile installs from here and then installs the application itself
# without dependencies, so a build cannot quietly resolve something else.
#
# To move a version: change it here deliberately, run the full suite and the
# evaluation suite, and commit the two together. Nothing in milestone 10
# upgraded anything; this is a snapshot of the tested set.
#
# `pytest` and the packages it brings (iniconfig, pluggy, Pygments) come
# from the `dev` extra installed in the virtualenv this was frozen from. They
# are not needed at run time — the image does not carry the test suite, since
# `tests/` is in .dockerignore — and they are left in rather than edited out
# so this file stays a faithful record of the set the suite ran on.
alembic==1.19.2
annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==1.5.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
docstring_parser==0.18.0
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore2==2.12.0
httpcore==1.0.9
httptools==0.8.0
httpx2==2.12.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jiter==0.16.0
Mako==1.4.1
MarkupSafe==3.0.3
packaging==26.3
pluggy==1.6.0
psycopg-binary==3.3.5
psycopg==3.3.5
pydantic-settings==2.15.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==9.1.1
python-dotenv==1.2.3
PyYAML==6.0.3
sniffio==1.3.1
SQLAlchemy==2.0.52
starlette==1.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
